# Electron Forge (asar packaging, @electron/rebuild, native module resolution)
# expects a flat node_modules. Use the hoisted linker so the layout matches
# what npm/yarn produced, avoiding symlink-related packaging issues.
node-linker=hoisted
